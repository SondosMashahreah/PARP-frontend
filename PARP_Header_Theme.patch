diff --git a/README.md b/README.md
index a36934d..a29f239 100644
--- a/README.md
+++ b/README.md
@@ -1,16 +1,48 @@
-# React + Vite
+# PARP Frontend
 
-This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.
+واجهة المنصة الفلسطينية للبحوث الإجرائية، باستخدام React وVite.
 
-Currently, two official plugins are available:
+## التشغيل
 
-- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
-- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)
+استخدمي إصدار Node.js المتوافق مع Vite الموجود في package.json، ثم:
 
-## React Compiler
+```bash
+npm ci
+npm run dev
+```
 
-The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).
+```bash
+npm run build
+npm run lint
+```
 
-## Expanding the ESLint configuration
+## الهيدر
 
-If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
+هيدر واحد: الشعار الحالي على اليسار، حقل البحث في الوسط، والإشعارات وزر القائمة على اليمين. ترتيب الهيدر مقصود حتى مع اتجاه الصفحة العربي.
+
+| الملف | المسؤولية |
+| --- | --- |
+| src/components/Header/Header.jsx | تركيب الهيدر وإدارة اللوحة المفتوحة |
+| src/components/Header/HeaderLogo.jsx | الشعار الموجود واسم المنصة |
+| src/components/Header/HeaderSearch.jsx | حقل البحث |
+| src/components/Header/Notifications.jsx | زر الإشعارات ولوحة الحالة الفارغة |
+| src/components/Header/Sidebar.jsx | القائمة الجانبية العمودية |
+| src/components/ui/Icon.jsx | الأيقونات المشتركة |
+| src/components/ui/IconButton.jsx | زر أيقونة قابل لإعادة الاستخدام |
+| src/styles/theme.css | ألوان الثيم والخطوط والحواف والمتغيرات العامة |
+
+لكل مكون ملف CSS مرافق. الألوان الأساسية داكنة مع درجات بنفسجية مستوحاة من المرجع البصري. الشعار الأصلي محفوظ كما هو في src/components/Header/img/logo.png.
+
+## نطاق هذه المرحلة
+
+- البحث يقبل الكتابة، ولا يستدعي API أو يعرض نتائج حتى الآن.
+- السايدبار فارغ عمدًا، باستثناء زر الإغلاق. لاحقًا يمكن إضافة محتواه داخل sidebar__content.
+- زر الإشعارات يعرض الحالة الفارغة، دون عدادات أو إشعارات تجريبية.
+- محتوى الصفحة الرئيسية فارغ وجاهز للخطوة التالية.
+- السايدبار يستخدم dialog أصليًا: يحجز التركيز داخله، ويغلق بزر الإغلاق أو Escape أو الضغط على الخلفية، ويعيد التركيز إلى زر الفتح.
+- دعم الشاشات الصغيرة، وأسماء عربية للأزرار، ومؤشرات التركيز، وتفضيل تقليل الحركة.
+- لم تتم إضافة مكتبات واجهة أو توجيه أو ربط بخدمات المتجر السابق.
+
+## ربط البيانات لاحقًا
+
+أضيفي اتصال البحث والإشعارات داخل ميزتهما، ثم مرري النتائج إلى مكونات العرض. صلاحيات البيانات تنفذ في الخلفية. تغيير الألوان العامة يتم من theme.css بدل تكرار القيم في الصفحات.
diff --git a/index.html b/index.html
index 1d3bbcf..3d73648 100644
--- a/index.html
+++ b/index.html
@@ -1,10 +1,12 @@
 <!doctype html>
-<html lang="en">
+<html lang="ar" dir="rtl">
   <head>
     <meta charset="UTF-8" />
-    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
+    <link rel="icon" type="image/png" href="/src/components/Header/img/logo.png" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-    <title>parp</title>
+    <meta name="theme-color" content="#0c0a13" />
+    <meta name="description" content="PARP — المنصة الفلسطينية للبحوث الإجرائية" />
+    <title>PARP | المنصة الفلسطينية للبحوث الإجرائية</title>
   </head>
   <body>
     <div id="root"></div>
diff --git a/src/App.css b/src/App.css
index e69de29..a95c24a 100644
--- a/src/App.css
+++ b/src/App.css
@@ -0,0 +1,20 @@
+.app-shell {
+  min-height: 100vh;
+  min-height: 100svh;
+  isolation: isolate;
+  background:
+    radial-gradient(ellipse at 10% 53%, rgb(121 56 189 / 17%), transparent 46%),
+    radial-gradient(ellipse at 88% 12%, rgb(110 54 172 / 12%), transparent 38%),
+    linear-gradient(155deg, #0d0b14 12%, #100c19 56%, #0b0911);
+}
+
+.app-main {
+  min-height: calc(100svh - var(--header-height));
+  width: min(var(--content-width), calc(100% - 80px));
+  margin-inline: auto;
+}
+
+@media (max-width: 640px) {
+  :root { --header-height: 86px; }
+  .app-main { width: calc(100% - 32px); }
+}
diff --git a/src/App.jsx b/src/App.jsx
index 92c0f54..4f4e55c 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -1,13 +1,12 @@
-import { useState } from 'react'
 import './App.css'
 import Header from './components/Header/Header.jsx'
 
 function App() {
-  const [count, setCount] = useState(0)
-
   return (
-    <div className="App">
+    <div className="app-shell">
+      <a className="skip-link" href="#main-content">انتقل إلى المحتوى</a>
       <Header />
+      <main id="main-content" className="app-main" tabIndex={-1} aria-label="محتوى المنصة" />
     </div>
   )
 }
diff --git a/src/components/Header/Header.css b/src/components/Header/Header.css
new file mode 100644
index 0000000..d624411
--- /dev/null
+++ b/src/components/Header/Header.css
@@ -0,0 +1,68 @@
+.site-header {
+  position: sticky;
+  top: 0;
+  z-index: 20;
+  background: rgb(12 10 19 / 78%);
+  -webkit-backdrop-filter: blur(24px);
+  backdrop-filter: blur(24px);
+}
+
+.site-header::after {
+  position: absolute;
+  right: 40px;
+  bottom: 0;
+  left: 40px;
+  height: 1px;
+  max-width: var(--content-width);
+  margin-inline: auto;
+  content: '';
+  background: linear-gradient(90deg, transparent, rgb(214 185 255 / 27%) 15%, rgb(214 185 255 / 12%) 85%, transparent);
+}
+
+.site-header__inner {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) minmax(240px, 420px) minmax(0, 1fr);
+  align-items: center;
+  gap: 28px;
+  width: min(var(--content-width), calc(100% - 80px));
+  min-height: var(--header-height);
+  margin-inline: auto;
+  direction: ltr;
+}
+
+.site-header__actions {
+  display: flex;
+  align-items: center;
+  justify-content: flex-end;
+  gap: 15px;
+  min-width: 0;
+}
+
+.site-header__divider { width: 1px; height: 24px; background: var(--color-border); }
+
+.site-header__menu {
+  border-color: var(--color-border-strong);
+  background: linear-gradient(140deg, rgb(180 129 255 / 17%), rgb(180 129 255 / 5%));
+}
+
+@media (max-width: 1100px) {
+  .site-header__inner {
+    grid-template-columns: minmax(0, 1fr) minmax(220px, 360px) minmax(0, 1fr);
+    gap: 20px;
+  }
+}
+
+@media (max-width: 760px) {
+  .site-header__inner {
+    grid-template-columns: auto minmax(0, 1fr) auto;
+    width: calc(100% - 40px);
+    gap: 20px;
+  }
+  .site-header__actions { gap: 8px; }
+  .site-header__divider { display: none; }
+}
+
+@media (max-width: 640px) {
+  .site-header__inner { width: calc(100% - 32px); gap: 12px; }
+  .site-header::after { right: 16px; left: 16px; }
+}
diff --git a/src/components/Header/Header.jsx b/src/components/Header/Header.jsx
index 06e3241..5b2199b 100644
--- a/src/components/Header/Header.jsx
+++ b/src/components/Header/Header.jsx
@@ -1,32 +1,42 @@
-import React from 'react'
-import logo from './img/logo.png'
-import link from 'react-router-dom'
+import { useCallback, useState } from 'react'
+import HeaderLogo from './HeaderLogo.jsx'
+import HeaderSearch from './HeaderSearch.jsx'
+import Notifications from './Notifications.jsx'
+import Sidebar from './Sidebar.jsx'
+import Icon from '../ui/Icon.jsx'
+import IconButton from '../ui/IconButton.jsx'
+import './Header.css'
 
-const Header = () => {
-  return (
-    <div>
-        <div className="left-header">
-            <div className="logo">   
-                <img
-                    src={logo}
-                    alt={t("Logo")}
-                />
-            </div>
-
-            <div className="title">
-                <h1>PARP</h1>
-                <h2>Palestinian Action Research Platform</h2>
-                <h2>المنصة الفلسطينية للبحوث الإجرائية</h2>
-            </div>
-
-        </div>
-
-        <div className="search-box">
-            
+function Header() {
+  const [activePanel, setActivePanel] = useState(null)
+  const closePanel = useCallback(() => setActivePanel(null), [])
 
+  return (
+    <header className="site-header">
+      <div className="site-header__inner">
+        <HeaderLogo />
+        <HeaderSearch />
+        <div className="site-header__actions">
+          <Notifications
+            isOpen={activePanel === 'notifications'}
+            onToggle={() => setActivePanel((current) => current === 'notifications' ? null : 'notifications')}
+            onClose={closePanel}
+          />
+          <span className="site-header__divider" aria-hidden="true" />
+          <IconButton
+            className="site-header__menu"
+            aria-label="فتح القائمة الجانبية"
+            aria-expanded={activePanel === 'sidebar'}
+            aria-controls="main-sidebar"
+            aria-haspopup="dialog"
+            onClick={() => setActivePanel('sidebar')}
+          >
+            <Icon name="menu" />
+          </IconButton>
         </div>
-
-    </div>
+      </div>
+      <Sidebar isOpen={activePanel === 'sidebar'} onClose={closePanel} />
+    </header>
   )
 }
 
diff --git a/src/components/Header/HeaderLogo.css b/src/components/Header/HeaderLogo.css
new file mode 100644
index 0000000..d0687c6
--- /dev/null
+++ b/src/components/Header/HeaderLogo.css
@@ -0,0 +1,76 @@
+.header-logo {
+  display: inline-flex;
+  align-items: center;
+  justify-self: start;
+  gap: 14px;
+  min-width: 0;
+  text-decoration: none;
+  border-radius: var(--radius-sm);
+}
+
+.header-logo__image-frame {
+  display: grid;
+  place-items: center;
+  flex: 0 0 58px;
+  width: 58px;
+  height: 60px;
+  padding: 4px;
+  overflow: hidden;
+  border: 1px solid rgb(213 188 255 / 40%);
+  border-radius: 16px;
+  background: white;
+  box-shadow: 0 0 24px rgb(170 117 255 / 10%);
+}
+
+.header-logo__image { width: 100%; height: 100%; object-fit: contain; }
+
+.header-logo__wordmark {
+  display: flex;
+  flex-direction: column;
+  align-items: flex-start;
+  min-width: 0;
+}
+
+.header-logo__name {
+  font-size: 25px;
+  font-weight: 650;
+  letter-spacing: 0.15em;
+  line-height: 1.15;
+}
+
+.header-logo__name > span { color: var(--color-primary); letter-spacing: 0; }
+
+.header-logo__subtitle {
+  margin-top: 5px;
+  color: var(--color-text-muted);
+  font-size: 9px;
+  letter-spacing: 0.035em;
+  white-space: nowrap;
+}
+
+.header-logo__arabic {
+  margin-top: 1px;
+  color: var(--color-text-subtle);
+  font-size: 10px;
+  white-space: nowrap;
+}
+
+@media (max-width: 1200px) {
+  .header-logo__subtitle, .header-logo__arabic { display: none; }
+}
+
+@media (max-width: 900px) {
+  .header-logo { gap: 10px; }
+  .header-logo__image-frame {
+    flex-basis: 44px;
+    width: 44px;
+    height: 46px;
+    padding: 3px;
+    border-radius: 12px;
+  }
+  .header-logo__name { font-size: 21px; letter-spacing: 0.08em; }
+}
+
+@media (max-width: 480px) {
+  .header-logo__wordmark { display: none; }
+}
diff --git a/src/components/Header/HeaderLogo.jsx b/src/components/Header/HeaderLogo.jsx
new file mode 100644
index 0000000..bda55d2
--- /dev/null
+++ b/src/components/Header/HeaderLogo.jsx
@@ -0,0 +1,17 @@
+import logo from './img/logo.png'
+import './HeaderLogo.css'
+
+export default function HeaderLogo() {
+  return (
+    <a className="header-logo" href={import.meta.env.BASE_URL} aria-label="PARP — الصفحة الرئيسية">
+      <span className="header-logo__image-frame">
+        <img className="header-logo__image" src={logo} alt="شعار المنصة الفلسطينية للبحوث الإجرائية" width="52" height="52" />
+      </span>
+      <span className="header-logo__wordmark">
+        <span className="header-logo__name" lang="en">PARP<span aria-hidden="true">.</span></span>
+        <span className="header-logo__subtitle" lang="en">Palestinian Action Research Platform</span>
+        <span className="header-logo__arabic" lang="ar" dir="rtl">المنصة الفلسطينية للبحوث الإجرائية</span>
+      </span>
+    </a>
+  )
+}
diff --git a/src/components/Header/HeaderSearch.css b/src/components/Header/HeaderSearch.css
new file mode 100644
index 0000000..b36e599
--- /dev/null
+++ b/src/components/Header/HeaderSearch.css
@@ -0,0 +1,44 @@
+.header-search {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+  width: 100%;
+  min-width: 0;
+  height: 48px;
+  padding-inline: 18px;
+  border: 1px solid var(--color-border);
+  border-radius: var(--radius-pill);
+  background: linear-gradient(110deg, rgb(151 110 191 / 7%), rgb(255 255 255 / 3%));
+  box-shadow: inset 0 1px 0 rgb(255 255 255 / 3%);
+  transition: border-color var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast);
+}
+
+.header-search:focus-within {
+  border-color: var(--color-primary);
+  background: rgb(167 117 235 / 9%);
+  box-shadow: 0 0 0 3px var(--color-primary-soft);
+}
+
+.header-search__icon { flex: 0 0 19px; width: 19px; height: 19px; color: var(--color-primary); }
+
+.header-search input {
+  width: 100%;
+  min-width: 0;
+  height: 100%;
+  padding: 0;
+  border: 0;
+  outline: none;
+  background: transparent;
+  color: var(--color-text);
+  font-size: 13px;
+  text-overflow: ellipsis;
+}
+
+.header-search input::placeholder { color: var(--color-text-subtle); opacity: 1; }
+.header-search input:focus-visible { outline: none; }
+
+@media (max-width: 640px) {
+  .header-search { height: 44px; gap: 8px; padding-inline: 12px; }
+  .header-search input { font-size: 16px; }
+  .header-search input::placeholder { font-size: 12px; }
+}
diff --git a/src/components/Header/HeaderSearch.jsx b/src/components/Header/HeaderSearch.jsx
new file mode 100644
index 0000000..dac60f1
--- /dev/null
+++ b/src/components/Header/HeaderSearch.jsx
@@ -0,0 +1,12 @@
+import Icon from '../ui/Icon.jsx'
+import './HeaderSearch.css'
+
+export default function HeaderSearch() {
+  return (
+    <form className="header-search" role="search" dir="rtl" onSubmit={(event) => event.preventDefault()}>
+      <label className="visually-hidden" htmlFor="header-search">البحث في المنصة</label>
+      <Icon name="search" className="header-search__icon" />
+      <input id="header-search" name="q" type="search" placeholder="ابحث في المنصة..." autoComplete="off" />
+    </form>
+  )
+}
diff --git a/src/components/Header/Notifications.css b/src/components/Header/Notifications.css
new file mode 100644
index 0000000..e11017f
--- /dev/null
+++ b/src/components/Header/Notifications.css
@@ -0,0 +1,54 @@
+.notifications { position: relative; }
+
+.notifications__panel {
+  position: absolute;
+  top: calc(100% + 22px);
+  right: 0;
+  width: 300px;
+  max-width: calc(100vw - 32px);
+  overflow: hidden;
+  border: 1px solid var(--color-border-strong);
+  border-radius: var(--radius-lg);
+  background: linear-gradient(145deg, #241a32, #15101e);
+  box-shadow: var(--shadow-panel);
+  text-align: start;
+}
+
+.notifications__heading {
+  margin: 0;
+  padding: 18px 22px;
+  border-bottom: 1px solid var(--color-border);
+  font-size: 15px;
+  font-weight: 600;
+}
+
+.notifications__empty {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 14px;
+  padding: 30px 20px;
+  color: var(--color-text-muted);
+}
+
+.notifications__empty-icon {
+  display: grid;
+  place-items: center;
+  width: 48px;
+  height: 48px;
+  border: 1px solid var(--color-border);
+  border-radius: 50%;
+  background: var(--color-primary-soft);
+  color: var(--color-primary);
+}
+
+.notifications__empty p { margin: 0; font-size: 13px; }
+
+@media (max-width: 640px) {
+  .notifications__panel {
+    position: fixed;
+    top: calc(var(--header-height) + 8px);
+    right: 16px;
+    width: min(320px, calc(100vw - 32px));
+  }
+}
diff --git a/src/components/Header/Notifications.jsx b/src/components/Header/Notifications.jsx
new file mode 100644
index 0000000..103b325
--- /dev/null
+++ b/src/components/Header/Notifications.jsx
@@ -0,0 +1,52 @@
+import { useEffect, useRef } from 'react'
+import Icon from '../ui/Icon.jsx'
+import IconButton from '../ui/IconButton.jsx'
+import './Notifications.css'
+
+export default function Notifications({ isOpen, onToggle, onClose }) {
+  const containerRef = useRef(null)
+  const buttonRef = useRef(null)
+
+  useEffect(() => {
+    if (!isOpen) return
+
+    function handlePointerDown(event) {
+      if (!containerRef.current?.contains(event.target)) onClose()
+    }
+
+    function handleKeyDown(event) {
+      if (event.key === 'Escape') {
+        onClose()
+        buttonRef.current?.focus()
+      }
+    }
+
+    document.addEventListener('pointerdown', handlePointerDown)
+    document.addEventListener('keydown', handleKeyDown)
+    return () => {
+      document.removeEventListener('pointerdown', handlePointerDown)
+      document.removeEventListener('keydown', handleKeyDown)
+    }
+  }, [isOpen, onClose])
+
+  return (
+    <div
+      className="notifications"
+      ref={containerRef}
+      onBlur={(event) => {
+        if (!event.currentTarget.contains(event.relatedTarget)) onClose()
+      }}
+    >
+      <IconButton ref={buttonRef} aria-label="الإشعارات" aria-expanded={isOpen} aria-controls="header-notifications" onClick={onToggle}>
+        <Icon name="bell" />
+      </IconButton>
+      <section id="header-notifications" className="notifications__panel" aria-label="الإشعارات" dir="rtl" hidden={!isOpen}>
+        <h2 className="notifications__heading">الإشعارات</h2>
+        <div className="notifications__empty">
+          <span className="notifications__empty-icon"><Icon name="bell" /></span>
+          <p>لا توجد إشعارات بعد</p>
+        </div>
+      </section>
+    </div>
+  )
+}
diff --git a/src/components/Header/Sidebar.css b/src/components/Header/Sidebar.css
new file mode 100644
index 0000000..cb020f6
--- /dev/null
+++ b/src/components/Header/Sidebar.css
@@ -0,0 +1,44 @@
+.sidebar {
+  position: fixed;
+  inset: 0 0 0 auto;
+  width: min(360px, calc(100vw - 28px));
+  max-width: none;
+  height: 100vh;
+  height: 100dvh;
+  max-height: none;
+  margin: 0 0 0 auto;
+  padding: 0;
+  overflow-y: auto;
+  overscroll-behavior: contain;
+  border: 0;
+  border-left: 1px solid var(--color-border-strong);
+  border-radius: var(--radius-lg) 0 0 var(--radius-lg);
+  background:
+    radial-gradient(ellipse at top right, rgb(160 98 228 / 12%), transparent 48%),
+    #15101e;
+  color: var(--color-text);
+  box-shadow: -24px 0 80px rgb(0 0 0 / 30%);
+}
+
+.sidebar[open] { animation: sidebar-enter 240ms ease-out; }
+
+.sidebar::backdrop {
+  background: rgb(5 3 10 / 64%);
+  -webkit-backdrop-filter: blur(5px);
+  backdrop-filter: blur(5px);
+}
+
+.sidebar__top {
+  display: flex;
+  align-items: center;
+  min-height: var(--header-height);
+  padding: 20px 24px;
+  border-bottom: 1px solid var(--color-border);
+}
+
+.sidebar__content { padding: 24px; }
+
+@keyframes sidebar-enter {
+  from { transform: translateX(100%); opacity: 0; }
+  to { transform: translateX(0); opacity: 1; }
+}
diff --git a/src/components/Header/Sidebar.jsx b/src/components/Header/Sidebar.jsx
new file mode 100644
index 0000000..9f4af38
--- /dev/null
+++ b/src/components/Header/Sidebar.jsx
@@ -0,0 +1,50 @@
+import { useEffect, useRef } from 'react'
+import Icon from '../ui/Icon.jsx'
+import IconButton from '../ui/IconButton.jsx'
+import './Sidebar.css'
+
+export default function Sidebar({ isOpen, onClose }) {
+  const dialogRef = useRef(null)
+
+  useEffect(() => {
+    const dialog = dialogRef.current
+
+    if (isOpen) {
+      if (!dialog.open) dialog.showModal()
+      const previousOverflow = document.body.style.overflow
+      document.body.style.overflow = 'hidden'
+      return () => {
+        document.body.style.overflow = previousOverflow
+      }
+    }
+
+    if (dialog.open) dialog.close()
+  }, [isOpen])
+
+  function handleBackdropClick(event) {
+    if (event.target !== event.currentTarget) return
+    const bounds = event.currentTarget.getBoundingClientRect()
+    if (event.clientX < bounds.left || event.clientX > bounds.right || event.clientY < bounds.top || event.clientY > bounds.bottom) onClose()
+  }
+
+  return (
+    <dialog
+      id="main-sidebar"
+      className="sidebar"
+      ref={dialogRef}
+      aria-label="القائمة الجانبية"
+      dir="rtl"
+      onClose={onClose}
+      onCancel={(event) => {
+        event.preventDefault()
+        onClose()
+      }}
+      onClick={handleBackdropClick}
+    >
+      <div className="sidebar__top">
+        <IconButton aria-label="إغلاق القائمة الجانبية" onClick={onClose}><Icon name="close" /></IconButton>
+      </div>
+      <div className="sidebar__content" />
+    </dialog>
+  )
+}
diff --git a/src/components/ui/Icon.jsx b/src/components/ui/Icon.jsx
new file mode 100644
index 0000000..85f9e87
--- /dev/null
+++ b/src/components/ui/Icon.jsx
@@ -0,0 +1,14 @@
+const paths = {
+  search: <><circle cx="10.75" cy="10.75" r="6.75" /><path d="m16 16 4.25 4.25" /></>,
+  bell: <><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9" /><path d="M10 21h4" /></>,
+  menu: <><path d="M4 6h16M8 12h12M4 18h16" /></>,
+  close: <><path d="m6 6 12 12M18 6 6 18" /></>,
+}
+
+export default function Icon({ name, className }) {
+  return (
+    <svg className={className} width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true" focusable="false">
+      {paths[name]}
+    </svg>
+  )
+}
diff --git a/src/components/ui/IconButton.css b/src/components/ui/IconButton.css
new file mode 100644
index 0000000..14f07ea
--- /dev/null
+++ b/src/components/ui/IconButton.css
@@ -0,0 +1,23 @@
+.icon-button {
+  display: inline-grid;
+  place-items: center;
+  flex: 0 0 44px;
+  width: 44px;
+  height: 44px;
+  padding: 0;
+  border: 1px solid var(--color-border);
+  border-radius: 14px;
+  background: rgb(255 255 255 / 2%);
+  color: var(--color-text-muted);
+  cursor: pointer;
+  transition: color var(--transition-fast), border-color var(--transition-fast), background var(--transition-fast), transform var(--transition-fast);
+}
+
+.icon-button:hover,
+.icon-button[aria-expanded='true'] {
+  border-color: var(--color-border-strong);
+  background: var(--color-primary-soft);
+  color: var(--color-text);
+}
+
+.icon-button:active { transform: scale(0.96); }
diff --git a/src/components/ui/IconButton.jsx b/src/components/ui/IconButton.jsx
new file mode 100644
index 0000000..240444a
--- /dev/null
+++ b/src/components/ui/IconButton.jsx
@@ -0,0 +1,5 @@
+import './IconButton.css'
+
+export default function IconButton({ children, className = '', ...props }) {
+  return <button type="button" className={['icon-button', className].filter(Boolean).join(' ')} {...props}>{children}</button>
+}
diff --git a/src/index.css b/src/index.css
index 4511b92..ce5a038 100644
--- a/src/index.css
+++ b/src/index.css
@@ -1,3 +1,69 @@
-:root {
-  --h1:color: #000000;
-}
\ No newline at end of file
+@import './styles/theme.css';
+
+*,
+*::before,
+*::after { box-sizing: border-box; }
+
+html {
+  min-width: 320px;
+  min-height: 100%;
+  background: var(--color-background);
+  scrollbar-gutter: stable;
+}
+
+body {
+  margin: 0;
+  color: var(--color-text);
+  font-family: var(--font-sans);
+  line-height: 1.6;
+  -webkit-font-smoothing: antialiased;
+}
+
+button, input { font: inherit; }
+button, a, input { -webkit-tap-highlight-color: transparent; }
+button, a { color: inherit; }
+img, svg { display: block; }
+
+button:focus-visible,
+a:focus-visible,
+input:focus-visible {
+  outline: 2px solid var(--color-focus);
+  outline-offset: 5px;
+}
+
+::selection { color: white; background: #7040a8; }
+
+.visually-hidden {
+  position: absolute;
+  width: 1px;
+  height: 1px;
+  padding: 0;
+  margin: -1px;
+  overflow: hidden;
+  clip-path: inset(50%);
+  white-space: nowrap;
+  border: 0;
+}
+
+.skip-link {
+  position: fixed;
+  inset-block-start: 12px;
+  inset-inline-start: 16px;
+  z-index: 100;
+  padding: 10px 18px;
+  border: 1px solid var(--color-focus);
+  border-radius: var(--radius-sm);
+  background: var(--color-surface);
+  transform: translateY(-200%);
+}
+
+.skip-link:focus { transform: translateY(0); }
+
+@media (prefers-reduced-motion: reduce) {
+  *, *::before, *::after {
+    animation-duration: 0.01ms !important;
+    animation-iteration-count: 1 !important;
+    transition-duration: 0.01ms !important;
+    scroll-behavior: auto !important;
+  }
+}
diff --git a/src/styles/theme.css b/src/styles/theme.css
new file mode 100644
index 0000000..1e11887
--- /dev/null
+++ b/src/styles/theme.css
@@ -0,0 +1,26 @@
+:root {
+  color-scheme: dark;
+
+  --color-background: #0c0a13;
+  --color-surface: #181321;
+  --color-surface-raised: #211a2e;
+  --color-text: #f6f1fd;
+  --color-text-muted: #b6abc9;
+  --color-text-subtle: #a296b6;
+  --color-primary: #b58aff;
+  --color-primary-strong: #9560ed;
+  --color-primary-soft: rgb(170 117 255 / 12%);
+  --color-border: rgb(205 178 255 / 15%);
+  --color-border-strong: rgb(205 178 255 / 32%);
+  --color-focus: #d6b9ff;
+
+  --font-sans: 'Segoe UI', 'Noto Sans Arabic', Tahoma, Arial, sans-serif;
+  --radius-sm: 10px;
+  --radius-md: 16px;
+  --radius-lg: 24px;
+  --radius-pill: 999px;
+  --shadow-panel: 0 24px 64px rgb(0 0 0 / 38%);
+  --transition-fast: 160ms ease;
+  --content-width: 1360px;
+  --header-height: 112px;
+}
